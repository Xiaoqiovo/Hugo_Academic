# name: Deploy to GitHub Pages

# on:
#   push:
#     branches:
#       - main  # 或者你的默认分支名

# jobs:
#   deploy:
#     runs-on: ubuntu-latest
#     steps:
#       - name: Checkout
#         uses: actions/checkout@v3
#         with:
#           submodules: true
#           fetch-depth: 0

#       - name: Setup Hugo
#         uses: peaceiris/actions-hugo@v2
#         with:
#           hugo-version: 'latest'
#           extended: true

#       - name: Build
#         run: hugo --minify

#       - name: Deploy
#         uses: peaceiris/actions-gh-pages@v3
#         with:
#           deploy_key: ${{ secrets.ACTIONS_DEPLOY_KEY }}
#           external_repository: Xiaoqiovo/Xiaoqiovo.github.io  # 替换为你的 GitHub Pages 仓库
#           publish_branch: main  # 或者 master，取决于你的默认分支
#           publish_dir: ./public
#           force_orphan: true  # 添加这行
#           commit_message: ${{ github.event.head_commit.message }}
